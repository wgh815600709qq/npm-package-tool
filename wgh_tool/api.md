## 方法说明

```
    基础类：
        _getVersion 获取基础库的版本信息
        

    判断类:
        _isObject 是否为对象
        _isMobile 是否为手机

    格式类：
        _parseTime 将时间对象转变成指定格式
        _formatTime 将时间对象转变成指定格式[包含时间间隔]

    目的类：
        _deepCopy 深拷贝
        _throttle 函数节流

```